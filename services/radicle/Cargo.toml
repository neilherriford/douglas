[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
docker = { path = "./docker" }
uds_utils = { path = "./uds_utils" }
clap = "4.5.32"
tokio = {version = "1.44.1", features = ["full"]}
