[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
docker = { path = "./docker" }
unix_domain_socket_http = { path = "./unix_domain_socket_http" }
clap = "4.5.32"
tokio = {version = "1.44.1", features = ["full"]}
